# Secrets — never commit
.env
credentials.json
token.json
*.key
*.pem

# Temporary / intermediate files (regenerated as needed)
.tmp/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# OS / editor
.DS_Store
.vscode/
.idea/

# Office / LibreOffice lock files (created while a document is open)
~$*

# Claude Code — personal/machine-local settings
.claude/settings.local.json
.claude/launch.json

# Local-only scratch (not part of the project)
lovable_prompt.md
